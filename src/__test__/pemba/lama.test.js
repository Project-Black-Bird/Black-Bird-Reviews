// const {sum, sayHello}

//if user clicks delete button, remove posts

// if the user clicks on the create a review button, the create a review form opens

// if the user enters the field, it keeps the reviews

// if the user clicks on the submit button, it saves the review

// when the user logs in, then only show the create a review button

// test('this is an example of what it should do', () => {
//     expect(().toBe(''));
// })

// test(`add returns NaN if non numbers are passed`, () => {
//   expect(add('hello', 'world')).toBeNaN();
// });

// test('object assignment', () => {
//     const data = {one: 1};
//     data['two'] = 2;
//     expect(data).toEqual({one: 1, two: 2});
// })

// test('expect true to be truthy', () => {
//     expect(true).toBeTruthy()
// })

// test('expect false to be falsy', () => {
//     expect(false).not.toBeTruthy()
// })

// let names = ['John', 'Ariel', 'Zach'];

// test('names contains Ariel', () => {
//     expect(names).toContain('Ariel')
//     expect(names).not.toContain('Matt')
// })
